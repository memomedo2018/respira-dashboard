name: Scheduled SEO Automation

on:
  schedule:
    # 09:00 Africa/Cairo during daylight saving time.
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Generate and publish daily blog article
        run: |
          curl --fail-with-body -X POST "https://perfect-art-production.up.railway.app/api/blog/generate" \
            -H "Content-Type: application/json" \
            -H "X-Admin-Password: ${{ secrets.RESPIRATECH_ADMIN_PASSWORD }}" \
            --data '{"count":1,"publish_now":true}'

      - name: Wait for generation thread
        run: sleep 180

      - name: Run SEO Brain
        run: |
          curl --fail-with-body -X POST "https://perfect-art-production.up.railway.app/api/seo/brain" \
            -H "Content-Type: application/json" \
            -H "X-Admin-Password: ${{ secrets.RESPIRATECH_ADMIN_PASSWORD }}" \
            --data '{"action":"full_run"}'
